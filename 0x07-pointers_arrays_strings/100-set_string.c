#include "main.h"

/**
 * set_string - function that set value of pointer to char
 * @s: double pointer to a string
 * @to: pointer to the string to set s
 * Return: Nothing
 */

void set_string(char **s, char *to)
{
	*s = to;
}
